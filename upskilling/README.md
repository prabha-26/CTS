# Upskilling Index

This folder contains the upskilling hands-on exercises.

| Module | Contents |
| --- | --- |
| Module 1 | HTML, CSS, Bootstrap, JavaScript, jQuery, images, and supporting assets. |
| Module 2 | SQL exercise output document. |
| Module 3 | C# ADO.NET console exercises 1-30. |

## Module 3 Notes

Each C# exercise is kept as a separate console project with its own `.csproj` and `Program.cs`.
Generated `bin/` and `obj/` folders are intentionally ignored so the repo stays source-focused.
