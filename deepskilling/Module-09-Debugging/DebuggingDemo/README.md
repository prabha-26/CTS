# Debugging Demo

Use this console application to practice:

- Breakpoints on object creation and loop iteration.
- Step Into, Step Over, and Step Out.
- Watch window expressions such as `order.Total`.
- Exception breakpoints by changing a quantity to a negative value.
- Inspecting calculated values before they are printed.

Run:

```bash
dotnet run
```
